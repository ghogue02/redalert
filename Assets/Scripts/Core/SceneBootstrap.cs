using UnityEngine;

namespace RedAlert.Core
{
    /// <summary>
    /// Entry point for scene initialization. Responsible for bringing up core services
    /// and preparing the game mode. Keep logic minimal until systems are implemented.
    /// </summary>
    public class SceneBootstrap : MonoBehaviour
    {
        // TODO: Initialize services (EventBus, ConfigService, PoolService) when implemented.

        private void Start()
        {
            // Placeholder bootstrap sequence.
        }
    }
}